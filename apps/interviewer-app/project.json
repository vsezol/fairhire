{
  "name": "interviewer-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "anticheat",
  "sourceRoot": "apps/interviewer-app/src",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/interviewer-app"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "interviewer-app:build",
        "host": "0.0.0.0"
      },
      "configurations": {
        "development": {
          "buildTarget": "interviewer-app:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "interviewer-app:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "interviewer-app:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "interviewer-app:build:development"
        },
        "production": {
          "buildTarget": "interviewer-app:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/interviewer-app/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/interviewer-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  }
}
