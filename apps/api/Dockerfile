# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @anticheat-interview/api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t anticheat-interview-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system anticheat-interview-api && \
          adduser --system -G anticheat-interview-api anticheat-interview-api

COPY apps/api/dist anticheat-interview-api/
COPY apps/api/package.json anticheat-interview-api/
RUN chown -R anticheat-interview-api:anticheat-interview-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix anticheat-interview-api --omit=dev -f install

CMD [ "node", "anticheat-interview-api" ]
