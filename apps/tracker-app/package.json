{
  "name": "@anticheat-interview/tracker-app",
  "version": "0.0.1",
  "license": "PolyForm-Internal-Use-1.0.0",
  "private": true,
  "nx": {
    "targets": {
      "build": {
        "executor": "@nx/vite:build",
        "outputs": [
          "{options.outputPath}"
        ],
        "defaultConfiguration": "production",
        "options": {
          "outputPath": "apps/tracker-app/dist"
        },
        "configurations": {
          "development": {
            "sourcemap": true,
            "declaration": true,
            "mode": "development"
          },
          "production": {
            "mode": "production"
          }
        }
      }
    }
  }
}
